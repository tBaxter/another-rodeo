---
layout: base.njk
---
<main>
  <div class="contact-layout">
    <div class="contact-intro">
      <h1>{{ title }}</h1>
      {% if description %}
      <p class="section-lead">{{ description }}</p>
      {% endif %}
    </div>
    {{ content | safe }}
  </div>
</main>